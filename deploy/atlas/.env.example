# Atlas CMMS Deployment Configuration for VPS 72.60.175.144
# Copy this file to .env and fill in the values

# ==================== Database ====================
POSTGRES_USER=atlas_admin
POSTGRES_PWD=CHANGEME_secure_postgres_password

# ==================== MinIO Storage ====================
MINIO_USER=minio_admin
MINIO_PWD=CHANGEME_secure_minio_password

# ==================== Security ====================
# IMPORTANT: Generate a strong JWT secret
# Example: openssl rand -base64 32
JWT_SECRET_KEY=CHANGEME_generate_random_jwt_secret_here

# ==================== Admin Access ====================
# Email addresses allowed to create organizations (comma-separated)
# Leave empty to allow anyone to create organizations
ALLOWED_ORGANIZATION_ADMINS=admin@rivet.com,tech@rivet.com

# ==================== Email Notifications (Optional) ====================
ENABLE_EMAIL_NOTIFICATIONS=false
INVITATION_VIA_EMAIL=false

# SMTP Configuration (only needed if email notifications enabled)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PWD=

# ==================== Notes ====================
# After deployment:
# 1. Access Atlas frontend: http://72.60.175.144:3000
# 2. Access Atlas API: http://72.60.175.144:8080
# 3. Access MinIO console: http://72.60.175.144:9001
# 4. First user to sign up becomes organization admin
# 5. Add admin email to ALLOWED_ORGANIZATION_ADMINS to allow signup
